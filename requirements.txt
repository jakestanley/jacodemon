appdirs
jsonpickle
obsws_python
pyqt6
PyQt6-WebEngine

# unix only

# windows only
win11toast; sys_platform == 'win32'
ahk; sys_platform == 'win32'
ahk[binary]; sys_platform == 'win32'

# mac only
hammerspoon-bridge; sys_platform == 'darwin'
